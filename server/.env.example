PORT=4000
DATABASE_URL="postgres://user:password@localhost:5432/skribbl_clone"
JWT_SECRET="replace-with-a-long-random-string"
CLIENT_ORIGIN="*"
# Grace period (ms) a disconnected player's seat + score are held for resume
RECONNECT_GRACE_MS=120000
